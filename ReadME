背景：
官网seo的版本，看到需求里面有张表格，需要把表格中的超链接地址和sitemap中的地址进行对比

目的：
如果一个个手动点击对比，效率很低，所以选择了用代码去进行比对

思路：
整体思路是，从Excel中拿到每一个链接，然后存储在列表或字典中，再去sitemap把这个页面所有的链接拿到，存在另一个列表或字典中，这样将两边进行对比，如果链接不同的地方，就是有问题的地方。

实现的步骤：
读取表格：
1、读取Excel表格；
2、能够获取到单个单元格的文字
3、能够获取到单个单元格的超链接
4、遍历循环整个表格，获取全部单元格的超链接
5、截取超链接，只用后半部分内容
6、组成字典

sitemap：
1、获取页面元素；
2、拿到标签中的文字
3、拿到标签中href属性的值
4、组成字典

调用两个文件中的方法，比较字典中不同内容，输出打印在CSV文件